# yaml-language-server: $schema=https://aka.ms/m365-agents-toolkits/v1.11/yaml.schema.json
version: v1.11
environmentFolderPath: ./env

provision:
  - uses: arm/deploy
    with:
      subscriptionId: ${{AZURE_SUBSCRIPTION_ID}}
      resourceGroupName: ${{AZURE_RESOURCE_GROUP_NAME}}
      bicepCliVersion: v0.30.23
      templates:
        - path: ./infra/azure.bicep
          parameters: ./infra/azure.parameters.json
          deploymentName: probtrk-${{TEAMSFX_ENV}}
  - uses: teamsApp/create
    with:
      name: probation-tracker${{APP_NAME_SUFFIX}}
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build
  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  - uses: teamsApp/extendToM365
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
    writeToEnvironmentFile:
      titleId: M365_TITLE_ID
      appId: M365_APP_ID

deploy:
  - uses: cli/runNpmCommand
    with:
      workingDirectory: src/mcpserver/server
      args: install --no-audit --no-fund
  - uses: cli/runNpmCommand
    with:
      workingDirectory: src/mcpserver/server
      args: run build
  - uses: cli/runNpmCommand
    with:
      workingDirectory: src/mcpserver/widgets
      args: install --no-audit --no-fund
  - uses: cli/runNpmCommand
    with:
      workingDirectory: src/mcpserver/widgets
      args: run build
  - uses: script
    with:
      run: node infra/stage.mjs
  - uses: azureAppService/zipDeploy
    with:
      artifactFolder: src/mcpserver/deploy-stage
      ignoreFile: .deployignore
      resourceId: ${{APPSERVICERESOURCEID}}

publish:
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build
  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  - uses: teamsApp/publishAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
    writeToEnvironmentFile:
      publishedAppId: TEAMS_APP_PUBLISHED_APP_ID
projectId: 99cd01f9-5c65-406e-9ab4-9c7461ad1392
